# Frontend (Vite) environment variables
# Copy to .env and adjust per environment

# When true, enables lightweight analytics tracking in the frontend
VITE_ENABLE_ANALYTICS=false

# Optional base URL for API calls in production (no trailing slash)
# Example: https://smart-sanitation-management-platform-2.onrender.com
VITE_API_BASE=
